package io.github.ausf_software.smbp_editor.render;

import javax.swing.*;
import java.awt.*;

public class ModePanel extends JPanel {
    public ModePanel() {
        setBackground(new Color(30, 30, 30));
        add(new JButton("mode"));
    }
}
